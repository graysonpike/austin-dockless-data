CREATE TABLE `scooters` (
	`Device ID`	TEXT NOT NULL,
	`Number of Trips`	INTEGER NOT NULL,
	`Total Duration of Trips (min)`	INTEGER NOT NULL,
	`Earilest Ride Timestamp`	TEXT NOT NULL,
	`Last Ride Timestamp`	TEXT NOT NULL,
	`Lifespan (days)`	INTEGER NOT NULL,
	`Total Distance (m)`	INTEGER NOT NULL,
	`Estimated Revenue`	INTEGER NOT NULL
);